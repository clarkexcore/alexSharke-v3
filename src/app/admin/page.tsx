import { Box } from "@mui/material";
import { Metadata } from "next";

export const metadata: Metadata = {
	title: "alexSHARKE - Admin",
};

export default function AdminPage() {
	return <Box>Hi - this is admin</Box>;
}
